export type FolderType = {
  folderId: number;
  title: string;
  // createdAt: string;
};

export type FolderMeetingType = {
  detailId: number;
  title: string;
  createAt: string;
  commentCnt: number;
  participantCnt: number;
  totalTime: string;
};
