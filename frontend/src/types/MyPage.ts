export type FolderType = {
  folderId: number;
  title: string;
  createAt: string;
};

export type FolderMeetingType = {
  detailId: number;
  title: string;
  createAt: string;
  commentCnt: number;
  participantCnt: number;
  totalTime: string;
};
